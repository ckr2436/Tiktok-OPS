[Service]
# API and Beat publish named tasks but never consume them. Loading every task
# module here multiplies provider SDK and model memory without registering a
# useful consumer.
Environment=GMV_CELERY_RUNTIME_ROLE=producer
