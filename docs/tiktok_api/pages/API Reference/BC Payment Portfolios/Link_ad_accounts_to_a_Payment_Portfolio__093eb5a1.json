{
  "title": "Link ad accounts to a Payment Portfolio",
  "endpoint": "https://business-api.tiktok.com/open_api/v1.3/payment_portfolio/advertiser/update/",
  "method": "POST",
  "category_path": [
    "BC Payment Portfolios"
  ],
  "official_only": true,
  "request": {
    "headers": [
      {
        "name": "Access-Token",
        "required": true,
        "type": "string",
        "raw_type": "string",
        "description": "Authorized access token. For details, see Authentication ."
      },
      {
        "name": "Content-Type",
        "required": true,
        "type": "string",
        "raw_type": "string",
        "description": "Request message type. Allowed format: application/json ."
      }
    ],
    "parameters": [
      {
        "name": "payment_portfolio_id",
        "required": true,
        "type": "string",
        "raw_type": "string",
        "description": "The ID of the Payment Portfolio. To obtain a list of Payment Portfolio IDs, use /payment_portfolio/get/ ."
      },
      {
        "name": "advertiser_ids",
        "required": true,
        "type": "array",
        "raw_type": "string[]",
        "description": "The list of ad accounts that you want to link to the Payment Portfolio. You can only link ad accounts that belong to the same client as the Payment Portfolio. Max size: 20. If the ad account belongs to a Business Center, you need to have Finance Manager or Admin access to the Business Center. To confirm your access to a Business Center, use /bc/get/ and check the returned user_role and finance_role . If the ad account does not belong to a Business Center, you need to have Admin or Operator access to the ad account. Note : If advertiser_ids contains any invalid IDs, the whole call will fail."
      }
    ],
    "examples": [
      {
        "title": "Example",
        "curl": "curl --location --request POST 'https://business-api.tiktok.com/open_api/v1.3/payment_portfolio/advertiser/update/' \\\n--header 'Access-Token: {{Access-Token}}' \\\n--header 'Content-Type: application/json' \\\n--data '{\n    \"payment_portfolio_id\": \"{{payment_portfolio_id}}\",\n    \"advertiser_ids\": [\"{{advertiser_id}}\"]\n}'"
      }
    ]
  },
  "response": {
    "top_fields": [
      {
        "name": "code",
        "required": false,
        "type": "number",
        "raw_type": "number",
        "description": "Response code. For the complete list of response codes and descriptions, see Appendix-Return Code ."
      },
      {
        "name": "message",
        "required": false,
        "type": "string",
        "raw_type": "string",
        "description": "Response message. For details, see Appendix - Return Codes ."
      },
      {
        "name": "request_id",
        "required": false,
        "type": "string",
        "raw_type": "string",
        "description": "The log ID of a request, which uniquely identifies the request."
      },
      {
        "name": "data",
        "required": false,
        "type": "object",
        "raw_type": "object",
        "description": "Returned data."
      }
    ],
    "data": {
      "fields": [],
      "list": {
        "item_fields": []
      }
    },
    "other_tables": [],
    "example": {
      "status_line": "HTTPS/1.1 200 OK",
      "body": "{\n    \"code\": 0,\n    \"message\": \"OK\",\n    \"request_id\": \"{{request_id}}\",\n    \"data\": {}\n}"
    }
  }
}
